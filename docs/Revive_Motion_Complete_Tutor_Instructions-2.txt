Revive MotionComplete Tutor InstructionsDennis and Michael Ozlem · Grade 7 · Brandon FL · MIT App Inventor · Twice-a-week sessionsThe one rule that matters most:Teach Dennis and Michael how the blocks work — do not build it for them. They must explain every part to science fair judges and Congressional App Challenge reviewers. If they cannot explain a block, back up and teach it first.All 10 Screens at a GlanceScreenNameTagWhat it is0Onboarding WizardNEWFirst-time setup: name+PIN, injury type, baseline ROM, schedule. Only shows once.1Session BriefingNEWPre-session: today's exercise, last session stats, PT updates. START SESSION button.2Patient DashboardCoreLive angle waveform, rep counter, EMG signal, ROM bar, exercise card.3Pain Check-InNEW1-10 pain scale before every session. Blocks session if 7+. Alerts caregiver.4Kids Game ModeCoreXP bar, avatar+speech bubble, quest, score+streak, badges, 3 mini-games.5Progress ChartsCoreROM line chart, compliance heatmap, weekly reps bars, pain trend.6Caregiver DashboardCoreStatus card, session history, dual-axis chart, before/now/goal, calendar.7RecoverAI CoachCore4 donuts, radar chart, AI analysis, exercise prescription card, physician report.8PT Remote UpdateNEWPhysical therapist updates exercise plan. Pushes to patient via CloudDB.9Exercise LibraryNEWExercises filtered by injury and ROM stage. Video links. PT-prescribed badge.PART 1 — THE PROJECTWhat Revive Motion IsRevive Motion is a wearable sensor device plus a phone app that Dennis and Michael are building for their science fair and the Congressional App Challenge. The device tracks physical therapy exercises automatically. The app shows the data live, keeps caregivers updated, and uses AI for personalized recommendations. It serves three groups: athletes recovering from sports injuries, kids who need daily PT exercises, and elderly patients who need large text and automatic reminders.The Three SensorsSensorMeasuresConnects viaSimulated byMPU-6050Joint angle, reps, hold time, range of motionArduino Nano → WiFiSlider 0–180MAX30102Heart rate — exercise effort levelArduino Nano → WiFiSlider 0–200MyoWare 2.0 EMGMuscle activation — correct muscle firingWireless Shield → WiFiSlider 0–100When sensors arrive:Arduino sends '45,82,67' = angle / heart rate / EMG. Use split text at block (split by comma). Replace each slider reference with the split value. Everything else stays the same.PART 2 — MIT APP INVENTOR vs AIWhat MIT App Inventor Handles AloneEverything below is built with blocks only. No external AI or API needed.FeatureHow it works in blocksOnboarding wizardScreen 0 with TinyDB. Check 'setup_complete' key on every launch.Pain check-in10 buttons, if pain>=7 block session and set CloudDB alert flag.Session briefingNew screen before live view. Loads from TinyDB and CloudDB.Live angle / HR / EMG displayLabel.Text updated by slider or WiFi data on every Clock tick.Green / amber / red feedbackIf/else chain: if pct>=1.0 green, if pct>=0.88 amber, else red.Amber colorblind patternCanvas.DrawLine diagonal stripes over feedback box when amber.Rep counterGlobal variable, increments when correct angle is held long enough.TinyDB — local storageStoreValue / GetValue. No internet. Survives app close.CloudDB — online syncSame blocks, requires internet. Shared across devices.Kids XP bar + levelsMath on global score. If/else chain for 5 level names.Avatar speech bubbleLabel positioned over avatar. Text changes per performance.Badges (4 total)If streak/ROM >= threshold, set Image.Picture to unlocked file.3 mini-gamesClock timers + variable counters + Canvas visuals.Weekly quest barLabel Width fills proportionally to weekly progress.ROM line chartChart component AddEntry per session end.Compliance heatmap4x7 grid of Image or Label components, color set in blocks.Caregiver status cardIf days_missed >= 2, set label text and show action buttons.Session history listListView loaded from CloudDB on Screen 6 initialize.Before/Now/Goal barsCanvas.DrawFilledRect with calculated heights.Session calendar4x7 grid, color per session value from CloudDB.Language toggle EN/ESTinyDB 'language' key. All label texts set on Screen Initialize.Exercise libraryList of exercises filtered by injury_type and current ROM.PT Remote UpdateScreen 8 with PT PIN. Saves to CloudDB 'exercise_prescription'.Physician emailWeb.PostText to IFTTT webhook. Auto-fires every Sunday.WiFi sensor dataWeb component receives text, split block separates 3 values.What AI Does (via Web Component)FeatureHow it worksPersonalized recommendationApp builds a text summary of patient data. Web.PostText sends to AI. GotText displays the response.Spanish (automatic)Add 'Please respond in Spanish.' to the prompt. Same API call. No translation API needed.Physician report narrativeApp sends weekly stats to AI. AI writes a 2-3 sentence clinical summary for the email.Smart exercise difficultyAI decides whether to increase difficulty, hold, or reduce based on compliance and ROM.Educational Q&APatient types a question. App sends to AI. AI explains in plain language, English or Spanish.Three AI Levels — Start SimpleLevelToolWhat to expect1 EASIESTChatBot componentBuilt into MIT App Inventor AI menu. No API key. Works in 10 minutes. Start here.2 MEDIUMOpenAI GPT APIFree account + API key. Web component POST. Full prompt control. Small monthly cost at high usage.3 ADVANCEDClaude or GeminiSame Web component approach. Gemini has free tier. Claude excels at clinical language.BONUSTeachable MachineTrain a pose model on correct/incorrect form photos. Import to App Inventor. Save for last.Start here in Session 9:Add the ChatBot component from the AI menu. Send patient data as text. Show the response in a Label. This takes 15 minutes. Then upgrade to OpenAI or Gemini if time allows.PART 3 — DESIGN RULESColor PaletteColorHexWhere to use itNavy#0d1b2eMain bg on Screens 2,4,7. App Inventor ARGB: &HFF0D1B2EBlue#2563ebAction buttons, active nav tabs, accent borders. &HFF2563EBGreen#10b981Correct form signal, ROM progress, positive stats. &HFF10B981Amber#d97706Almost-there state (88-99% of target). Diagonal pattern for colorblind. &HFFd97706Red#ef4444Wrong form, missed days, high pain, alerts. &HFFEF4444Orange#f59e0bPoints, streaks, kids accents, quest highlight.Purple#8b5cf6AI screen, EMG data, RecoverAI module.Gold#fbbf24XP bar marker, crowns, trajectory position, personal best.Dark purple#0a0520Kids Mode background ONLY. Signals game mode.Off-white#f0f4ffCaregiver background ONLY. Signals clinical context.Font RulesSizeUsed forSettings52ptAngle number, score, rep countBold ON. Color changes with feedback state.28ptStats: reps, HR, EMGBold ON. Color matches the data type.22ptScreen title, patient nameBold ON. White text.18ptButton labelsBold ON. White text.16ptBody text, exercise cardBold OFF. Grey (#94a3b8). Minimum allowed.13ptSmall hints, chart labelsBold OFF. Grey. Nothing smaller than this.Non-Negotiable RulesRemove ALL grey backgrounds. Set every BackgroundColor explicitly. Grey = unfinished.FeedbackBox: minimum 160px tall, full width. Green / Amber / Red. Most important element in the app.Amber state (88-99%): text says 'ALMOST THERE!' not red. Diagonal Canvas lines for colorblind users.All buttons: minimum 80px tall. Elderly and kids need big tap targets.All text: minimum 16pt. Key numbers at 52pt. No exceptions.Test on a real phone after every session. The browser preview is unreliable.TitleVisible = unchecked on all screens. Use a custom label instead.Kids Mode background: #0a0520. Signals game mode. Never use navy on Kids screen.Caregiver background: #f0f4ff. Signals clinical, trustworthy context.Ask after every screen: could an elderly person use this without any help?PART 4 — REALISTIC TIME ESTIMATESTwice-a-week sessions, 60–120 minutes each. Total: approximately 16–20 hours across 11 sessions. Budget 7–8 weeks.S#SessionTimeWhy that long0Onboarding + PIN setup90 minNew screen, TinyDB, 5-step wizard. First time building multi-step flows.1Screens, Colors, Layout90 minFirst session in App Inventor. Interface is new. Do not rush.2Simulated Data + Feedback90 min3-state feedback (green/amber/red) requires careful if/else chain.3TinyDB + Pain Scale90 minTwo features combined. TinyDB is new concept. Pain scale adds conditional blocking.4Kids Game Mode2-3 hrsMost complex session. Split across 2 tutor sessions. Badge images, streak dates, levels, speech bubble.5ROM Chart + Progress Screen90 minChart component and heatmap together. Data structure must be right before chart works.6Exercise Recommendations + Library90 minIf/then by angle and injury type. Exercise library with video links.7CloudDB + Caregiver2 hrsTwo-device testing. Cloud sync debugging is common. Status card + action buttons.8Physician Report + Spanish2 hrsIFTTT setup is new territory. Translating every label takes careful work.9RecoverAI AI Module90 minChatBot first (15 min). Upgrade to API if time allows. JSON parsing.10PT Remote Update90 minNew screen with PT PIN. CloudDB prescription key. Patient briefing screen reads it.11Sensor Swap-In90 minReplace sliders with WiFi data. Split text. Hardware debugging.TOTALAll sessions16-20 hrs7-8 weeks twice a week. Add buffer for slower sessions.PART 5 — SESSION INSTRUCTIONSSessions 1–10 need no sensors. Session 11 connects real sensor data. Session 0 is the new onboarding wizard.Before Session 0: students create accounts at appinventor.mit.edu and download the MIT AI2 Companion app.SESSION 0 — Onboarding Wizard + Patient PIN · 90 min★ Goal: Screen 0 exists. 5-step onboarding works. PIN stored. injury_type saved. Baseline ROM measured. setup_complete flag set.Build:Create Screen 0 named OnboardingScreen. Set bg to navy.Check TinyDB 'setup_complete' on Screen 1 Initialize. If 'yes', skip Screen 0 and go to Screen 1.Step 1: welcome screen with 3 benefit cards (track exercises, keep caregiver informed, see progress). Large 'Let's Go!' button.Step 2: name field + 4-digit PIN (10 number buttons). Store 'patient_name' and 'patient_pin' in TinyDB. PIN = CloudDB key.Step 3: injury type selector. 4 buttons: Knee, Ankle, Elbow, Wrist. Store 'injury_type' in TinyDB.Step 4: baseline ROM. Patient bends joint to current max. Hold 3 seconds. Read angle slider. Save as 'baseline_rom' AND as first item in 'rom_history' list.Step 5: schedule. 3 days / 4 days / 5 days per week. Morning / Afternoon / Evening. Save as 'session_days' and 'session_time'.Summary screen: 'Your starting angle: 42 degrees. Goal: 90 degrees. 48 degrees to recover. Let's go!'Set TinyDB 'setup_complete' = 'yes'. Open Screen 1.Teach:TinyDB.StoreValue and GetValue — tag is the key name, value is the dataScreen.Open to navigate between screensIf TinyDB.GetValue = 'yes' — how to check a stored string valueProcedure: how to create a reusable block group and call it from multiple placesLists: make a list, add items to list, length of list, select list item🎨 Design:Onboarding bg: navy. Benefits shown as full-width cards with icon area on left.'Let's Go!' button: green gradient, 12pt bold white. Friendly language throughout.SESSION 1 — Screens, Colors, and Layout · 90 min★ Goal: All 10 screens exist. Colors correct. Fonts set. App runs live on phone.Build:Create project 'ReviveMotion' in MIT App Inventor. Connect AI2 Companion on both phones.Screen 1 (Session Briefing): bg navy. Header with patient name and day number. Exercise card area. Start Session button.Screen 2 (Patient): bg navy. Header. FeedbackBox 160px, FontSize 52, Bold ON, Width=Fill parent. 3 stat cards row. ROM bar. Exercise card.Screen 3 (Pain): bg navy. Title centered. 10 number buttons in 2 rows of 5.Screen 4 (Kids): bg #0a0520. Header with EN/ES toggle. XP bar. Avatar area. Score/streak row. Quest box. Rep button. Badges row.Screen 5 (Charts): bg navy. Tab row (ROM / Reps / Pain). Chart areas with placeholder labels.Screen 6 (Caregiver): bg #f0f4ff. Dark navy header. Status card. Session list. Chart areas. Send Report button.Screen 7 (AI Coach): bg navy. Header with EN/ES toggle. 4 donut placeholders. Radar placeholder. Generate button. Results area.Screen 8 (PT Update): bg navy. PT Portal header. Patient stats. Exercise selector. Metrics. Notes. Push button.Screen 9 (Exercise Library): bg navy. Filter tabs. Exercise card list.Test every screen on phone. TitleVisible = unchecked on all. No grey anywhere.Teach:Custom hex colors in MIT App Inventor: ARGB format &HFF + hex (e.g. #0d1b2e = &HFF0D1B2E)Screen properties: BackgroundColor, TitleVisible (always unchecked), AlignHorizontalHorizontalArrangement, VerticalArrangement — how to organize components in rows and columnsAI2 Companion: QR code scanning, live preview on real phoneComponent naming: name every component clearly (AngleLabel, FeedbackBox, RepBtn) before building blocks🎨 Design:No grey anywhere after this session. Every BackgroundColor must be set.Upload Revive Motion logo to Media. Add to header of every screen.Kids bg: #0a0520 (very dark purple). Caregiver bg: #f0f4ff (off-white).SESSION 2 — Simulated Data + 3-State Feedback · 90 min★ Goal: Drag slider, angle updates, feedback shows green/amber/red correctly, rep counter goes up.Build:Add three sliders to Screen 2: AngleSlider (0-180), HRSlider (0-200), EMGSlider (0-100).When AngleSlider changes: set AngleLabel.Text to round(AngleSlider.ThumbPosition).Set global targetAngle = 90. Calculate pct = AngleSlider.ThumbPosition / targetAngle.3-state: if pct >= 1.0 set FeedbackBox green + text 'CORRECT FORM'. Else if pct >= 0.88 set amber + text 'ALMOST THERE!'. Else set red + text 'ADJUST FORM'.For colorblind: when amber state, use Canvas.DrawLine to draw diagonal stripes across the feedback box.Add CountRepBtn (Height=80, bg blue). When clicked AND angle >= targetAngle: add 1 to global repCounter, update RepLabel.Repeat for HR and EMG sliders: update their labels when sliders move.Test: drag slider through all three zones. Colors must change correctly.Teach:Event blocks: when Component.Event do — fires every time that event happensGlobal variables: initialize global, set global, get globalIf/then/else chain: how to check multiple conditions in the right orderMath blocks: divide, >=, roundCanvas: DrawLine for colorblind pattern (when amber, draw 10 diagonal lines across feedback box)Set property blocks: set Label.Text to, set Label.BackgroundColor to🎨 Design:FeedbackBox: Height=160, Width=Fill, FontSize=52, Bold=ON, TextAlignment=Center.Green = #10b981, Amber = #d97706, Red = #ef4444. Use exact hex values.Amber text: 'ALMOST THERE!' not red. Patients should not feel they failed at 88 degrees.SESSION 3 — TinyDB + Pain Scale · 90 min★ Goal: Rep count survives app close. Pain scale blocks high-pain sessions and alerts caregiver.Build:Add TinyDB component. Screen 2 Initialize: load 'reps_today'. If empty set repCounter = 0, else set repCounter to saved value.When repCounter changes: TinyDB.StoreValue 'reps_today' repCounter.New Session button: append reps to 'session_history' list. Save today's date as 'last_session_date'. Reset counter.Pain scale on Screen 3: 10 buttons. When button tapped: save pain_level, check if >= 7.If pain >= 7: show red warning screen. Set CloudDB key 'pain_alert_[patient_pin]' = 'yes'. Block session (do not open Screen 2).If pain < 7: save pain score with session data. Open Screen 1 (Session Briefing).Caregiver Screen 6: on initialize, check CloudDB 'pain_alert_[patient_pin]'. If 'yes', show amber warning banner.Test: enter pain 8. Confirm session blocked and caregiver screen shows alert.Teach:TinyDB: StoreValue, GetValue, tag namesIs value empty? block: always check before using TinyDB data or app crashesAppending to a list: add items to list blockClock.Now and FormatDate: getting today's date as a text stringCloudDB: how it differs from TinyDB (online, shared across devices)Screen.Open: navigating to a different screen based on pain levelSESSION 4 — Kids Gamification Module · 2–3 hrs (split if needed)★ Goal: Full game mode: XP bar, avatar with speech bubble, quest front-and-center, score, streak, badges, 3 mini-games, celebrations.Build:XP bar: rr across top. Fill width = (score/1000) * pw. Updates every time points are awarded.5 levels: 0-99 = Benched, 100-299 = On the Bench, 300-599 = Back in Practice, 600-999 = Starting Lineup, 1000+ = MVP. Change level label text and color in blocks.Avatar speech bubble: a Label positioned to the right of the avatar. Text changes: if correct rep = 'Great form! Keep it up [name]!', if missed = 'You can do it [name]!', if level up = 'You leveled up!'Points: correct rep = 10pts. Perfect form (angle AND hold) = 20pts. Full session = 50pts. Daily goal = 100pts. Create AwardPoints(amount) procedure.Streak: compare last_session_date to yesterday. If yesterday: streak+1. If today: keep. Else: reset. Save today.4 badges: First Rep (any correct rep), 3-Day Warrior (streak>=3), Week Champion (streak>=7), Range Master (ROM>=goal). If earned: set Image.Picture to unlocked file, show Notifier celebration.Mini-game 1 Angle Challenge: random target angle (random integer 30-120). 5 rounds. Score by accuracy.Mini-game 2 Hold It!: 10-second countdown Clock. Canvas DrawRect shrinks as timer counts. Gold star if held full time.Mini-game 3 Rep Race: 60-second Clock. Max correct reps. Compare to TinyDB personal best. New best = 200pt bonus.Daily Quest: TinyDB 'quest_target' = 10 reps. Quest bar fills proportionally. 200pt bonus when complete.Spanish: check 'language' TinyDB key. All level names, badge names, quest text, mini-game labels switch.Teach:Procedure with parameter: AwardPoints(amount) — how to create and call a procedure with an inputDate arithmetic: subtract 86400000ms (24 hours) from Clock.Now to get yesterdayRandom integer block: generates random target angle for Angle ChallengeJoin text: builds dynamic speech bubble message with patient nameImage.Picture: switches between locked and unlocked badge image filesCanvas.DrawFilledRect: Hold It! shrinking timer bar; confetti DrawPoint at random positionsNested if/else for 5 levels: check from highest to lowestNotifier.ShowAlert: badge unlock and level-up celebrations🎨 Design:Kids bg: #0a0520. Deepest dark purple. Nothing else.Avatar: 88pt emoji centered. Speech bubble to upper-right. White box, orange border, tail pointing to avatar.XP bar: narrow (8px height) at very top. Orange fill. Gold dot at current position.Quest box: bordered in purple, prominent, above the rep button. Not buried below badges.Rep button: full width, green, 42px tall, inner highlight strip. 'TAP TO COMPLETE REP' in white 10pt bold.Score/streak: side by side, equal weight. Both in large 26pt numbers with colored borders.SESSION 5 — Progress Charts Screen · 90 min★ Goal: ROM line chart, compliance heatmap, weekly reps bars, and pain trend all working on Screen 5.Build:ROM line chart: load 'rom_history' list from TinyDB. Chart component AddEntry for each session. X = session number. Y = degrees. Show green goal line at target ROM.Missed sessions: if value = 0 in the list, mark with a different color dot (red) at the bottom.Compliance heatmap: 4x7 grid of Labels. Load last 28 days from CloudDB. Color each cell: dark green=full session, medium=partial, light=missed.Weekly reps bars: 7 bars (Mon-Sun). Load this week's daily reps from CloudDB. Color: green if target met, orange if partial, red if zero.Pain trend: load last 14 pain scores. Line chart. Color of line changes based on trend direction.Tab row at top: ROM / Reps / Pain. When tab tapped, show/hide relevant chart using Visible property.Before/Now/Goal chart on Caregiver screen (Screen 6): Canvas.DrawFilledRect for each bar. Heights calculated from ROM values.Test: add 5-7 fake session entries and confirm all charts populate correctly.Teach:Chart component: AddEntry, SetDomain, SetRange, set Type to LineVisible property: set Label/Chart Visible to true or false to show/hide sectionsCanvas.DrawFilledRect: for bars in the before/now/goal chartIterating a list: for each item in list do — how to loop through stored session dataLength of list: use to calculate chart scale and number of data pointsCloudDB.GotValue: how to load data and wait for the async response before populating charts🎨 Design:All chart backgrounds: match screen background color. Charts should feel embedded not pasted.Goal line: green dashed, labeled 'Goal' at the right end.Heatmap colors: very dark to bright green. Empty cells = near-black (#1a2f4a).ROM chart: blue line, blue-fill area below, red dot at baseline for missed sessions.SESSION 6 — Exercise Recommendations + Library · 90 min★ Goal: If/then exercise recommendation working. Exercise Library (Screen 9) filtered by injury and ROM.Build:Recommendation on Screen 2: calculate average angle from rom_history. If < 30 deg = 'Gentle pendulum swings'. If 30-59 = 'Seated knee bends'. If 60-89 = 'Resistance band extensions'. If 90+ = 'Functional movement exercises'.Load from CloudDB 'exercise_prescription' key first. If PT has set a plan, show that instead of the if/then recommendation.Exercise Library (Screen 9): create a list of exercises with name, sets, reps, hold time, video link, and minimum ROM for each.Filter: only show exercises where minimum ROM <= patient's current ROM AND injury type matches TinyDB 'injury_type'.Prescribed badge: if an exercise name matches CloudDB 'exercise_prescription', show a 'Prescribed by PT' label on that card.Video link: when 'Watch Demo' button tapped, use ActivityStarter or WebViewer to open YouTube link.Spanish versions: all recommendation text and exercise names must switch when language = 'es'.Test: set ROM to different values. Confirm only appropriate exercises appear.Teach:Average calculation: sum list items then divide by length of listNested if/else with ranges: if >= 30 AND < 60 (use AND block)Lists of lists: how to store exercise data as structured recordsFiltering a list: for each item, check if condition met before displayingActivityStarter: how to open a URL in the device browserCloudDB GotValue priority: PT prescription overrides AI recommendation🎨 Design:Exercise card: full-width, left-side color stripe matching injury color. Name in bold, sets/reps below.Prescribed badge: green pill in top-right corner of card.Video button: small pill shape, right side. 'Watch' not 'Watch Demo' (shorter on small cards).SESSION 7 — CloudDB + Caregiver Dashboard · 2 hrs★ Goal: Session data saves online. Caregiver Screen 6 reads it live on a second device with full chart display.Build:CloudDB token setup: tutor creates token at clouddb.appinventor.mit.edu.After each session: CloudDB.StoreValue tag = patient_pin + '_sessions', value = list of date/reps/angle/HR/EMG/pain.Screen 6 initialize: CloudDB.GetValue with patient tag. GotValue event populates session ListView and all charts.Status card: count missed days in last 7 days. If >= 2: show 'NEEDS ATTENTION' in large orange text + Send Reminder button + Call button. If < 2: show 'ON TRACK' in green.Send Reminder button: use Web component to send push notification via IFTTT or a simple SMS via TextMagic.Dual-axis ROM+Pain chart: ROM as solid blue line, pain as dashed red line on Canvas. Show inverse relationship.Before/Now/Goal bars: baseline ROM, current ROM, and goal ROM as three Canvas bars with color and height from values.Calendar heatmap: 4x7 Label grid, color each cell by session quality from CloudDB.Test on two phones simultaneously. End session on Phone 1. See it update on Phone 2 caregiver screen.Teach:CloudDB vs TinyDB: same blocks, CloudDB is online and accessible from any device with the tokenGotValue event: cloud data is not instant — this event fires when the server respondsCanvas dual-axis: how to normalize two different scales (degrees vs pain 1-10) to the same chart heightError handling: if CloudDB fails, show 'Connection error. Data may be delayed.' — do not crashTwo-device testing: both must use same CloudDB token and same patient PIN key🎨 Design:Caregiver bg: #f0f4ff. Clinical, professional, trustworthy.Status card: full-width, rounded, colored border (orange=needs attention, green=on track). Large text 12pt+.Action buttons IN the status card. Not separate. 'Send Reminder' and 'Call [name]' side by side.Session rows: left-side color bar (3px), dot, date bold, stats in grey, status emoji or OK/!/X on right.SESSION 8 — Physician Report + Spanish · 2 hrs★ Goal: Weekly report auto-sends every Sunday. Full Spanish mode works across all screens.Build:IFTTT setup: tutor creates account + Webhooks applet. Patient to Gmail. Provide students with the webhook URL.Build report text: 'Week of [date]: [reps done] of [reps prescribed]. Avg angle [X] deg. ROM gain [X] deg this week. Pain avg [X]/10. RecoverAI: [AI summary].'Send Report button on Screen 6: Web.PostText to IFTTT URL. Notifier confirms sent.Auto-send: Clock component, TimerInterval = 7 days (604800000ms). On fire: check if today = Sunday using Clock.WeekdayNumber. If 1 (Sunday), trigger send.Language button: top-right on every screen. Toggle TinyDB 'language' key between 'en' and 'es'.Every Screen Initialize: if language = 'es', set all visible text labels to Spanish versions. Create SetLanguage procedure for each screen.Translate: all buttons, feedback messages ('FORMA CORRECTA' / 'CASI! / 'CORRIGE TU FORMA'), level names, badge names, quest text, exercise names, caregiver status text, physician report.AI prompt: append 'Please respond in Spanish.' when language = 'es'. AI response comes back in Spanish automatically.Test: switch to Spanish. Walk every screen. Every visible word must be Spanish. Switch back: all English.Teach:Web component: URL, PostText, GotText — how HTTP POST works from App InventorIFTTT Webhooks: tutor demonstrates creating the applet from scratchClock.WeekdayNumber: returns 1 for Sunday through 7 for SaturdayLanguage variable pattern: load from TinyDB on every Screen Initialize, set all labelsSetLanguage procedure per screen: cleaner than scattered if/else throughout the blocksJoin text: building the report string by combining values with static text🎨 Design:Language button: top-right, every screen. Flag emoji or 'EN'/'ES' text. Small, unobtrusive.Spanish button text must match button size. 'FORMA CORRECTA' is longer than 'CORRECT FORM' — reduce font slightly if needed.SESSION 9 — RecoverAI Coach — AI Module · 90 min★ Goal: App calls AI, returns personalized recommendation in English or Spanish, shown with full visual layout.Build:Start with ChatBot component (Level 1): add from AI Extensions menu. No API key. Send patient summary. Show response in AIResultLabel. Done in 15 minutes.Then upgrade to Web component (Level 2): add Web component. Tutor provides API key as global variable.Build patient summary: 'Patient completed [X] of 10 reps. Avg angle [X] deg. ROM improved [X] deg this week. Pain avg [X]/10. Streak: [X] days.'If language = 'es': append 'Please respond in Spanish.' to the prompt.Web.GotText: extract text from JSON using JsonTextDecode block. Display in AIResultLabel.Exercise prescription card: parse AI response to extract exercise name, sets, reps. Display as 4 visual pills (Sets / Reps / Hold / Target).Trajectory bar: calculate (current_ROM / target_ROM) * 100 = completion percent. Set bar width proportionally. Show gold marker.Recovery radar: plot 5 values (compliance, ROM, strength estimate, effort, pain inverse) on Canvas as pentagon shape.4 donut metrics: draw 4 circular progress arcs using Canvas.DrawArc for compliance, ROM, weekly gain, effort.Physician report: append AI summary to report text before sending via IFTTT.Test with different simulated data. AI should give different responses for different compliance levels.Teach:ChatBot component: AskQuestion block, GotResponse event — from AI Extensions menuWeb component: URL, PostText, GotText, Headers block for API authorizationJsonTextDecode: extracts the text content from an API JSON responseCanvas.DrawArc: how to draw partial circles for donut charts (start angle, sweep angle)Canvas.DrawPolygon: how to draw the radar chart as a filled polygonAPI key security: store as global variable, never in a visible label or heading🎨 Design:AI Coach bg: navy. Purple accents for everything AI-related.Generate button: full width, 44px tall, purple-to-blue gradient (use uploaded image as background).AI result label: FontSize 14, color #e2e8f0, bg #1e3a5f, rounded corners via Canvas.Exercise prescription card: green background, white text. 4 pills in a row with sets/reps/hold/target.Trajectory bar: narrow (10px), purple fill, gold dot at current position, 42d and 90d labels at ends.SESSION 10 — PT Remote Update · 90 min★ Goal: Physical therapist can update the patient's exercise plan from their own device. Patient sees it on next briefing.Build:Add Screen 8 (PTScreen). Password-protected with a separate PT PIN (different from patient PIN).PT PIN: a simple text entry + button. Compare to a hardcoded PT PIN or one stored by the clinic admin.PTScreen loads patient data from CloudDB: compliance rate, ROM, streak, last 7 sessions.Exercise selector: Spinner component with list of exercise names (from exercise library).Metrics inputs: 4 TextBox components for sets, reps, hold time, target angle.PT notes: TextBox component for free-text notes to the patient.Push Update button: build a dictionary-style list [exercise_name, sets, reps, hold, angle, pt_notes, date_updated]. CloudDB.StoreValue tag = patient_pin + '_prescription', value = that list.Screen 1 (Session Briefing) initialize: load '_prescription' from CloudDB. If it exists, show 'PT UPDATE' banner with the new exercise name and notes.Screen 2 (Patient) exercise card: load prescription and use it instead of the AI recommendation if present.Test: update plan on Screen 8 as PT. Then open Screen 1 as patient. PT update banner must appear.Teach:Spinner component: a dropdown selector. How to set the Spinner.ElementsFromString with exercise namesTextBox: how to read TextBox.Text in blocks after user typesPIN comparison: if TextBox.Text = stored_PT_PIN, unlock the screenCloudDB key structure: patient_pin + '_prescription' as a compound keyScreen 1 Initialize: check CloudDB for prescription before rendering briefing. GotValue fires asynchronously.Visible property: show/hide the PT update banner based on whether prescription exists🎨 Design:PT screen: navy bg. TEAL accents to distinguish from patient screens.PT Portal label at top in teal, bold. Clearly not a patient screen.Push button: teal, full width, 42px. 'Push Update to Patient' label.Patient-side update banner: teal left-side border, shows exercise name and PT notes in navy card.SESSION 11 — Connect Real Sensor Data · 90 min★ Goal: Replace sliders with real WiFi sensor data. Everything else works automatically.Build:Arduino sends text string over WiFi: '45,82,67' = angle, heart rate, EMG percent.Add Web component or use existing one. Set URL to Arduino WiFi endpoint.Clock polling: TimerInterval = 500ms (twice per second). TimerAlwaysFires = checked.On Clock.TimerFired: Web.Get the Arduino URL.Web.GotText: split text at comma. Split gives a list of 3 items.Select item 1 = angle string. Convert text to number. Replace AngleSlider.ThumbPosition reference in blocks.Select item 2 = heart rate. Convert text to number. Replace HRSlider reference.Select item 3 = EMG percent. Convert text to number. Replace EMGSlider reference.Test each sensor one at a time. Angle first. Move joint slowly. Confirm feedback box responds.Everything else: gamification, caregiver, AI, charts, Spanish, PT prescription — all work automatically.Teach:split text at: splits '45,82,67' into a list ['45','82','67']Text to number: split values are strings. Must convert before math operations.Clock polling vs event-driven: why we poll for sensor data on a timerDebug label: temporarily show raw incoming text in a label to confirm correct format before replacing slidersWiFi troubleshooting: Arduino and phone must be on same WiFi networkPART 6 — ALL COMPONENTS NEEDEDMIT App Inventor Component ListComponentWhat it does in Revive MotionLabelAngle number, rep count, feedback text, speech bubble, status messages. Most-used component.Slider x3Simulates angle (0-180), heart rate (0-200), EMG (0-100) until sensors arrive.ButtonCount Rep, New Session, Start Session, Send Report, Language toggle, Pain scale buttons (10), mini-game buttons.TinyDBSaves locally: reps, score, streak, badges, level, language, rom_history, patient_name, injury_type, baseline_rom, setup_complete, personal_best.CloudDBSaves online: session history (shared with caregiver), pain_alert, exercise_prescription, family leaderboard.ClockToday's date, streak comparison, mini-game timers (Hold It!, Rep Race), Sunday auto-report, WiFi polling.ChartROM line chart on Screen 5. Type=Line. X=session number. Y=degrees.CanvasFeedback colorblind pattern (diagonal lines), EMG waveform, donut arcs, radar chart, bar charts, heatmap, confetti.Web componentAI API calls (ChatBot fallback to Web), IFTTT email webhook, WiFi sensor data polling.ChatBotLevel 1 AI — no API key needed. From AI Extensions menu. RecoverAI Coach recommendations.NotifierBadge unlock alerts, level-up announcements, report confirmation, high-pain session blocked message.ImageBadge images (4 locked + 4 unlocked = 8 total). Avatar option images. Revive Motion logo. Gradient button bg.SpinnerExercise selector dropdown on PT Remote Update screen.TextBoxPT notes input, PT PIN entry, patient name entry.ListViewSession history list on Caregiver screen.Multiple Screens (10+)Onboard, Briefing, Patient, Pain, Kids, Charts, Caregiver, AI Coach, PT Update, Exercise Library.ActivityStarter / WebViewerOpens exercise demo video links in browser or in-app viewer.When Stuck — Search TermsProblemSearch for this exact phraseHex colors not workingMIT App Inventor custom color ARGB format3-state feedback (amber) not triggeringMIT App Inventor if else chain blocks tutorialColorblind diagonal linesMIT App Inventor Canvas DrawLine tutorialTinyDB data not savingMIT App Inventor TinyDB StoreValue GetValue tutorialCloudDB not updating second deviceMIT App Inventor CloudDB GotValue event tutorialROM chart not showing dataMIT App Inventor Chart component AddEntry tutorialDonut arc chartMIT App Inventor Canvas DrawArc tutorialRadar chart (pentagon)MIT App Inventor Canvas DrawPolygon tutorialChatBot not respondingMIT App Inventor ChatBot AI extension tutorialAPI call not workingMIT App Inventor Web component PostText GotText tutorialEmail not sendingIFTTT Webhooks MIT App Inventor tutorialSpanish not switchingMIT App Inventor global variable language Screen InitializeExercise library filterMIT App Inventor filter list for each item tutorialSensor data not splittingMIT App Inventor split text at block tutorialApp crashes when loadingMIT App Inventor is value empty? block tutorialTwo phones not syncingMIT App Inventor CloudDB token shared devices tutorialSpeech bubble positioningMIT App Inventor Canvas DrawText positioned label tutorialPT update not showing on briefingMIT App Inventor CloudDB GotValue Screen Initialize tutorialVideo link not openingMIT App Inventor ActivityStarter open URL tutorialImages to Make in Canva (free at canva.com)Revive Motion logo 400x100px PNG — text + heartbeat icon, navy bg. Upload to header of every screen.Gradient button bg 600x60px PNG — blue (#2563eb) to purple (#8b5cf6) left-to-right. For Generate button on AI Coach screen.Badge 1 locked 100x100px PNG — dark grey circle + lock icon. Used before badge is earned.Badge 1 unlocked 100x100px PNG — colored circle + medal. Set Image.Picture when streak reaches 1st rep.Badges 2, 3, 4 100x100px PNG each — repeat for 3-Day, 7-Day, and ROM badge. 8 image files total.US Flag 30x20px PNG — EN toggle. Free from flaticon.com.Spain Flag 30x20px PNG — ES toggle. Free from flaticon.com.Revive Motion · Complete Tutor Instructions · Dennis & Michael Ozlem · Grade 7 · Congressional App Challenge 2026